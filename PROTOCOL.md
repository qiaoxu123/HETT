# 10 m 粗网格目标定位验证

- 划分：`val_unseen`，不读取 `test_unseen`。
- 样本：4 张地图，每图固定随机抽取 20 个不同 episode；每个 episode 取最接近轨迹中点且目标在图内的帧，共 80 条。
- 模型：Qwen2.5-VL-3B + 已冻结的 step3000 LoRA。该检查点与当前公开可视化页面一致，不根据本实验结果选模。
- 输入：同一张 100×100 m、896×896 航向对齐正射图。
- 配对条件：
  1. 原有 bbox 输出的中心点；
  2. 将同一中心点量化到 10×10 网格中心；
  3. 提示模型直接输出 10×10 网格行列。
- 主指标：Hit@20。次指标：Hit@10、NE 中位数、P90、无效输出率。
- 判据：若条件 2 相比条件 1 的 Hit@20 下降不超过 3 个百分点，则 10 m 网格表示足够；条件 3 只验证现有 LoRA 能否零样本切换输出接口，不据此否定后续热力图训练。

