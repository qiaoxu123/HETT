# 多目标假设验证记录

日期：2026-09-11

## 实现

该方案默认关闭。开启后，原有 5×5 target logits 不再只作为训练辅助量，而是形成 25 个全局目标假设：

- 每个候选格预测格内偏移，避免直接飞向约 82 米宽网格的角点或中心。
- tracker 只累计当前和历史模型 logits，不访问未来观测或真实目标。
- 保留概率最高的 3 个候选用于诊断，当前动作采用 MAP 假设，避免多个相距很远的目标求平均。
- 证据按时间衰减，后续强烈矛盾观测可以推翻早期错误目标。
- 熵接近最大值的均匀预测权重为 0，重复观看相似但无信息的画面不会凭次数制造置信度。
- 每步保存 top-k 索引、概率和证据置信度，便于检查模型是否真的发生假设更新。

## 已完成验证

CPU 测试 14/14 通过：

- 正确保留排序后的多个候选。
- 均匀预测重复输入仍保持均匀，不产生虚假确信。
- 连续的矛盾强证据能够改变 MAP 目标。
- 非有限输入和错误候选数会显式报错。
- teacher 独立计数回归测试继续通过。
- 完整 ET 已用 batch=2、240×240 地图执行前向和反向；25×2 偏移输出及历史 token 形状正确，偏移头全部参数获得有限梯度。

## 尚未完成

- 单卡前向、偏移 loss 梯度和显存检查。
- 与原连续 target head 使用同一 checkpoint/训练预算的闭环对照。
- 检查 top-k 是否有真实多样性、错误 MAP 是否会被后续证据纠正。
- 完整 unseen SR/SPL 和失败案例。

目前只证明时序证据逻辑成立，不能声称多假设能提高导航效果。若 grounding＋恢复已经明显有效，本方案应保持为备选消融，避免把论文主线做散。
