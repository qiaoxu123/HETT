# HETT 隔离实验与验证顺序

基准提交：`51a1828`。当前主目录及其后台训练保持不变。
此文档以 `00-control` 分支版本为准；各实验目录初始副本不会自动同步。

## 目录与职责

所有目录位于 `/home/tenant2/Workspace/hett-experiments/`。

| 顺序 | 目录 | 分支 | 目的 |
| --- | --- | --- | --- |
| 0 | 00-control | codex/experiment-control | 公共诊断工具、实验台账；不改模型 |
| 1 | 01-teacher-fix | codex/fix-teacher-index | 修复 teacher 批内共用轨迹计数，建立修复后基线 |
| 2 | 02-recovery | codex/verify-recovery | 可恢复阶段切换与独立到达判断 |
| 3 | 03-grounding | codex/verify-grounding | 保留局部视觉信息，学习语言与目标区域对应 |
| 4 | 04-combined | codex/verify-grounding-recovery | 验证定位与恢复是否互补 |
| 5 | 05-hypotheses | codex/verify-multi-hypothesis | 备选：多目标假设与观测证据更新 |
| 6 | 06-bidir | codex/verify-bidirectional-attention | 独立验证双向注意力，不与其他结构混算 |
| 7 | 07-loss-ablation | codex/verify-loss-ablation | 独立验证 progress 与额外 target-grid loss，不与结构改动混算 |

## 隔离与公平性约定

- 各目录有独立源码、runs、checkpoints；禁止共用输出目录。
- data、weights 仅共享输入，按只读使用；禁止通过链接修改数据或预训练权重。
- 共用现有 Python 环境仅用于运行；依赖变更必须另建环境。
- 所有分支从公共诊断工具提交起步，不自动包含其他分支未来修改。
- 第 1 步通过后，将明确的修复提交 cherry-pick 到后续分支，并记录哈希；组合分支只接收已验证提交。
- 单卡串行执行。现有基线运行结束前，不启动新的全量训练；不停止、不覆盖正在运行的实验。
- 区分代码修复、训练采样改进和模型创新，分别消融，不能把修复收益算作创新收益。

## TODO（只有真实完成才打勾）

- [x] 建立公共诊断工具快照，保留主目录未提交脚本。
- [x] 验证所有 worktree、输入路径、独立输出目录以及主目录状态；诊断脚本语法检查通过。
- [x] 审计数据划分：跨 split 无 episode key/起点重复，val/test unseen 地图与训练隔离；开发评估默认不加载 test。
- [x] 建立串行、失败即停的 GPU 短跑队列；当前等待原版基线正常结束。
- [x] 1a：teacher 计数单元测试：batch=1/2、不同 episode 提前结束、轨迹越界；每条轨迹独立推进。
- [ ] 1b：修复 teacher 索引；CPU 回归测试及单卡短跑，检查有限 loss、梯度、保存恢复。
- [ ] 1c：修复后基线完整训练/评估，固定数据、种子、预算、损失权重；与旧运行分开报告。
- [x] 2a：恢复机制状态机测试：误切换、重新进入粗阶段、越过目标、错误停止。
- [ ] 2b：只改控制机制的消融；新增恢复训练采样另做对照，不混算。
- [x] 3a：局部区域定位、不可见目标输出；检查无 GT 坐标、语义库存、未来图像进入推理。
- [ ] 3b：同地标不同目标、打乱语言、打乱视觉、关闭新模块，验证确实利用目标语义。
- [ ] 4：组合已验证的定位和恢复模块，与单模块使用同一训练协议比较。
- [ ] 5：若主要方案效果不足，再验证多假设方案；不在测试集反复选结构。
- [ ] 最终：至少 3 个训练种子，完整 unseen 集，按地图配对统计及置信区间，保存失败案例和绘图。
- [ ] 双向注意力：整网双向梯度已验证；待真实数据单卡开/关短训及完整对照。
- [ ] Loss 消融：四组权重协议与命令行已验证；待真实数据短跑及独立完整训练。
- [x] 预声明完整 seed-0 矩阵：修复基线完成后串行运行 11 个全量训练/评估任务，并对每项主张建立配对比较；默认不加载 test。
- [x] 预声明三种子确认矩阵：seed 17、42 各运行 10 个全量任务，与 seed 0 合并后覆盖所有核心主张；每项任务启动前保留 16 GiB 磁盘安全门。
- [x] 三种子汇总器：不挑最佳 seed，自动保存均值、样本标准差、95% 区间、逐 seed 配对变化，并绘制总体指标和配对差值图。
- [x] 冻结后最终测试协议：先按三种子 val-unseen 的预声明门槛写入 `freeze.json`，之后才显式读取 test；只比较冻结方案与修复基线，不再根据 test 调整。
- [x] 阶段行为诊断：从已有轨迹派生切换次数、切换时真实目标距离、stage1/stage2 动作数、progress 提前停止及停止误判，不要求改模型或重跑已有预测。
- [x] 训练动态汇总：7 个训练方案 × 3 seeds 自动汇总 loss、SR/SPL/NE 曲线、加权 loss 组成、耗时、最佳 epoch 与峰值显存；纯评估控制策略不混入训练曲线。

## 每次实验必须保存

在本 worktree 的 `runs/<唯一实验名>/` 保存：提交哈希与未提交差异、配置、种子、输入/划分标识、权重来源、运行命令、环境版本、逐项原始及加权 loss、训练耗时和显存、最终逐 episode 轨迹与预测。

指标：SR、SPL、NE、oracle SR、阶段切换次数、切换时误差、到过目标但最终失败比例、实际执行动作数、停止误判率；局部定位另报目标可见率、可见/不可见分组误差。离线预测坐标命中率不能叫闭环导航成功率。

快速试验仅排错，不能证明有效。候选方案进入完整实验的参考门槛为 unseen SR 提升约 3 个百分点且 SPL 无明显退化，但这不是统计显著性标准。验证集用于开发，测试集仅用于冻结方案后的最终报告。

## 已有证据与局限

- 已发现 teacher 的 stage1_step 在 batch 内共用；独立计数修复已通过 CPU 回归，导航收益尚未验证。
- 阶段切换后不能返回，以及“曾到达但最后失败”，支持测试恢复机制；不代表它必然提升。
- 早期冻结模型的轻量几何探针没有可靠收益，不能据此断言点云无用。
- grounding 的区域监督在 train_seen 有 63.49% 可见，在 val_unseen 有 46.88% 可见；不可见样本使用 outside 类，不用未来图像补标签。
- grounding checkpoint 与 combined 参数表完全一致；关闭 grounding / 多假设只丢弃各自新增参数。双向注意力开关不改变参数表。
- 双向注意力两条方向均有非零有限梯度，且开关会改变四个任务头输出；这仍不等于导航效果已证实。

已有几何实验报告（只读参考）：
`/home/tenant2/Workspace/hett-crotonyl/runs/local_geometry_probe_20260911/REPORT.md`

## 实验结果台账

| 实验 | 代码哈希 | 父基线/修复哈希 | 配置/种子 | 输出目录 | 结论 |
| --- | --- | --- | --- | --- | --- |
| 隔离环境建立 | 公共工具 e582afb；隔离配置见各分支后续提交 | 51a1828 | 不训练 | 各 worktree | 输入链接和输出隔离检查；不代表模型验证完成 |
| 跨方案 checkpoint 兼容性 | `b6107e4` | 各方案最新提交 | 不训练 | `runs/checkpoint_compatibility_20260911/` | grounding→combined 精确兼容；关闭模块时仅有预期的新模块参数被忽略 |
| 论文参数一致性 | `d3bef88` | 六个结构分支 | 不训练 | `runs/training_protocol_audit_20260911/` | 六分支协议一致；论文核心参数匹配；单卡用 2×累积4，另标明发布代码额外 target-grid loss |
| 数据划分审计 | `5821366` | 最新 CityNav 输入哈希 | 不训练 | `runs/dataset_split_audit_20260911/` | 四个 split 无目标描述键/起点重叠；两个 unseen 地图集合与训练隔离 |
| Loss 消融协议 | `9b50ad4` | `b01add8` | 四组权重；seed 0 起步 | `07-loss-ablation/runs/` | 16/16 CPU 测试通过；真实训练未开始 |
| 总队列监控 | `c3c0c76` | 八个后台服务 | 60 秒心跳 | `runs/chain_monitor_20260911/` | 同时检查服务状态与最终结果文件；时间戳变化不会重复记录事件 |
| 完整 seed-0 矩阵 | `140a4f8` | 修复后完整基线 | seed 0；20 epochs；全量 train/val | `runs/full_seed0_matrix_20260911/` | 11 个任务和 11 组配对比较已由测试锁定；等待前置运行 |
| 三种子确认矩阵 | `04b5c19`；汇总器 `6d71cdf` | 完整 seed-0 矩阵 | seeds 17/42；20 epochs；全量 train/val | `runs/multiseed_confirmation_20260911/` | 20 个任务已由测试锁定；加上 seed 0 后每项主张均有 3 个种子 |
| 冻结后最终测试 | `8918a76` | 三种子 val-unseen 汇总 | 先冻结，后首次读取 test | `runs/frozen_final_test_20260911/` | 无候选过门槛则冻结修复基线；禁止根据 test 重新选型 |
