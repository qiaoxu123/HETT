# 地标到达与 teacher 轨迹诊断

## 结论

用户的判断成立：当前模型经常到过地标附近，但没有可靠地确认到达，也没有以“到达地标”为阶段切换条件。

`val_unseen` 全量 2,697 条：

| 指标 | 10 m | 20 m |
|---|---:|---:|
| 轨迹曾进入地标范围 | 90.54% | 96.96% |
| 切入第二阶段时仍在范围内 | 60.03% | 80.77% |
| 到达后又离开范围 | 26.80% | 11.17% |
| 到过地标后的最终目标成功率 | 19.63% | 19.29% |

这说明主要问题不是“完全找不到地标”，而是**缺少可靠的到达确认和到达后的局部控制**。

## 当前输出能否确认到达

当前网络没有 landmark-arrival head：

- 阶段切换条件是当前位置距离模型自己预测的最终目标小于 5 m；
- `progress` 的真值是到最终目标的距离，不是到地标轮廓的距离；
- 把现有 progress 临时当作“到达地标”分类器，阈值只在 `val_seen` 选择后，`val_unseen` 的状态级平衡准确率为 67.70%（10 m）和 76.54%（20 m），不足以作为可靠回复。

已加入不改变策略的地图到达输出：每一步保存 `landmark_distance_m`、`landmark_arrived`，每条轨迹保存 `landmark_arrival_confirmed` 和 `landmark_first_arrival_step`。它直接使用已提供的 pose 和地标轮廓，因此对“是否进入给定地图范围”是确定计算。验证集地标名称到地图对象的规范化精确匹配率为 val_seen 100%、val_unseen 99.93%。两条未见集缺少地标的样本不会误报到达。

该输出只能确认地图位置，不能证明视觉编码器正确识别了地标。此前配对消融中，RGB 置零只使 128 条样本的 SR 从 18.75% 降到 14.06%，首次目标误差几乎不变（48.36 m → 48.10 m），也支持当前策略主要依赖地图轮廓，而没有充分使用视觉完成地标确认。

## teacher 的实际问题

当前 teacher 并不是清晰的“地标前/地标后”两段监督：

- 第一阶段每次直接跳到人类轨迹的第 `stage1_step × move_iteration` 个位置；主分支的 `stage1_step` 还是 batch 共用标量。
- teacher 观测每步有 50% 概率随机改 yaw，破坏了人类原始观察方向。
- 方向监督始终直接指向最终目标，不指向人类轨迹的下一观察点。
- 人类轨迹没有按地标到达点切分；阶段一结束仍由“距最终目标 5 m”决定。

在 `val_unseen` 的人类 teacher 轨迹中，以 20 m 为到达标准：87.76% 会到达地标；首次到达后仍有中位数 70 m，占整条路径 29.41%，此时距最终目标中位数仍为 52.89 m。这段才是应重点整理的局部探索监督。

之前直接把整条人类轨迹替换成直线 teacher 已做过 512 条配对筛查，`val_unseen` SR 反而下降 1.17 个点。因此不能简单丢弃人类轨迹。

## 建议的最小下一步

1. 保留新加的地图到达输出，先用它作为高可靠阶段门；连续两步进入 10–20 m 范围才确认，防止边界抖动。
2. 用关系热图最高区域选择地标轮廓的相关局部段，避免在长道路或大轮廓的错误位置过早确认。
3. 预处理 human teacher：在首次稳定到达点切成两段；到达前保留平滑粗导航，到达后保留能增加新视野或持续接近目标的节点，删除重复点、回环和无新增观察的移动。
4. 下一轮只测试 teacher 重标和阶段门，不先改主干网络。新增视觉确认头应作为第二步，输出“地图已到达”和“视觉已识别”两个独立置信度，避免把 GPS/轮廓命中误称为视觉理解。

全量逐轨迹结果保存在 `runs/landmark_gate_audit*/results.json`。两条样本的集成冒烟评测通过，新增输出长度与保存轨迹长度一致。
