# 轮廓匹配预验证（2026-09-15）

结论：普通 RGB 边缘与地图轮廓直接比较，没有显示可靠的定位区分能力。
这不是建筑语义分割实验，也不是导航成功率实验。

## 方法与边界

- 固定 val_unseen 四张地图，按地图名轮流取原始顺序的 10 个不同目标对象；每条人工记录轨迹取起点、中点、终点，共 30 帧，统一离地 50 米。不是当前模型实际 rollout。
- 仅当前 224×224 RGB 输入 Canny（先 5×5 Gaussian，默认阈值 50/150）。既有具名地标轮廓只用于随后评分，不提示边缘提取。
- 使用原工程 raster/crop 的同一映射将既有地标投影到画面。没有真实目标坐标、目标掩码、深度、全图 RGB 特征或训练。
- 比较正确投影与 8 个固定错位版本（横纵方向 ±16 像素，含对角线）。全部版本用相同边界采样点，距离图像边缘至少 18 像素。
- 去掉与图像边缘相连、面积至少 100 像素的全黑无数据区，并膨胀 4 像素；所有候选及翻转负对照采用共同有效区域。黑色内部纹理不当作无数据区。
- 指标是边界点距 RGB 边缘不超过 2 像素的比例，至少 20 个有效边界点才计分。不是物体匹配准确率。没有人工可见性标注，不能把未通过门槛的帧称为地标不可见。

## 结果

30 帧中 15 帧有足够有效边界，共 19 个“帧－地标”组合。

| 边缘阈值 | 正确边界贴合率 | 错位边界平均贴合率 | 正确边界严格胜过全部 8 个错位版本 |
|---|---:|---:|---:|
| 默认 50/150 | 59.34% | 57.90% | 3/19 |
| 较低 30/90 | 74.18% | 70.52% | 2/19 |
| 较高 80/200 | 49.23% | 44.53% | 3/19 |

默认设置只有 8/19 组合超过其错位对照均值。水平翻转 RGB 后，正确位置的贴合率仍达 59.73%，说明大量边缘容易产生偶然贴合。
轨迹等权平均增益 -0.61 个百分点；按轨迹 bootstrap 的描述性 95% 区间 [-8.86, 8.64] 个百分点，样本小且区间跨零。bootstrap 不是多随机种子训练。

## 怎样理解

图像边缘包含树冠、阴影、屋顶纹理、道路等，不等于完整建筑轮廓；地图多边形也不一定紧贴可见屋顶。
因此不能直接把普通边缘当作语义轮廓用于导航。下一步若继续，应先做建筑/道路区域分割，再验证对应关系。
本次没有验证地标身份匹配正确率、不可见时误报率、语言作用和最终导航收益；这些不能由此结果代替。

## 可复现与资料

运行 `python scripts/probe_contour_evidence.py`，测试 `python scripts/test_contour_evidence.py`，使用 AirVLN39 环境。
最终结果：`runs/contour_probe_balanced/results.json`；全部 RGB：`frame_00.png` 至 `frame_29.png`。
`examples.png` 按帧平均增益自动展示最大、中位、最小案例；`all_frames.png` 展示全部样本；`comparison.png` 为阈值对照图。
初次单地图探针保留于 `runs/contour_probe/`，不作为主结果。随后检查修正采样覆盖和无数据边界处理，未依结果优化阈值。
独立分支 `codex/verify-contour-evidence`；未修改模型、训练任务或其他 worktree。
